-- invoking setup() defines a keybinding (default "<leader>s") for web search and gives the possibility to configure
-- optional configurations.
--require("websearch").setup()

-- without invoking setup(), no keybinding is defined, but the ex mode command `Websearch` is available.
require("websearch")
